fun main(){
    //condicionales

    //if
    if(23 > 0){
        //print("correcto")
    }
    if(45 == 45){
        //print("es iguales")
    }

    //if y else
    if(23 > 0){
        //print("correcto")
    }else{
        //print("no correcto")
    }
    //if anidado
    if(23 > 0){
        //print("correcto")
    }else if(45 > 0){
        //print("correcto")
    }else{
        //print("no correcto")
    }

    //if ternario
    // 23>0 ? lo verdaro: lo falso
    //print(if (34>0 && 34<50) "correcto34" else "incorrecto")

    //case
    //swith //
    // when
    var a= 2
    var b=0
    var c=4

    when{
        //a>b && a>c -> print(a)
        //b>a && c>b -> print(b)
        //else -> print(c)
        
        a>b && a>c && b>c -> print("$a, $b, $c, el menor es $c")
        a>b && a>c && b>c -> print("$a, $b, $c, el menor es $c")
        a>b && a>c && b>c -> print("$a, $b, $c, el menor es $c")
    }
    //mostrar de mayo a menor. y motsra el menor.

    //terminar cuale s mayor de 3 numeros con when.

    /*swit(option)
    case (a>b and  a>c)
    case 2
    case 3 ...
    brenk*/




}